# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type ReservationType {
  id: ID!
  userId: String!
  roomId: String!
  startTime: String!
  endTime: String!
  createdAt: String!
}

type accessTokenType {
  accessToken: String!
}

type UserType {
  id: ID!
  keycloak_id: String!
  email: String!
  created_at: String!
  reservations: [ReservationType!]
}

type RoomType {
  id: ID!
  name: String!
  capacity: Int!
  location: String!
  created_at: String!
  reservations: [ReservationType!]
}

type Query {
  listReservations(skip: Int, limit: Int): [ReservationType!]!
  reservation(id: ID!): ReservationType
  listUsers(skip: Float!, limit: Float!): [UserType!]!
  room(id: ID!): RoomType
  login(email: String!, password: String!): accessTokenType!
  listRooms(skip: Int, limit: Int): [RoomType!]!
}

type Mutation {
  createReservation(userId: Int!, roomId: Int!, startTime: String!, endTime: String!): ReservationType!
  updateReservation(id: ID!, userId: Int, roomId: Int, startTime: String, endTime: String): ReservationType!
  deleteReservation(id: ID!): Boolean!
  createRoom(name: String!, capacity: Int!, location: String!): RoomType!
  updateRoom(id: ID!, name: String, capacity: Int, location: String): RoomType!
  deleteRoom(id: ID!): Boolean!
}